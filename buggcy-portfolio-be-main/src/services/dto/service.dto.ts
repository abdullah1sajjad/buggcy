/**
 * Service DTOs are derived from the Zod schemas.
 * Import types from here; import schemas from ../schemas/service.schema.
 */
export type { CreateServiceDto, UpdateServiceDto, ServiceFilterDto } from '../schemas/service.schema';
