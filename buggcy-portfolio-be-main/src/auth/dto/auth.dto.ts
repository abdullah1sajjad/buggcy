/**
 * Auth DTOs are derived from the Zod schemas.
 * Import types from here; import schemas from ../schemas/auth.schema.
 */
export type { LoginDto, ForgotPasswordDto, ResetPasswordDto, JwtPayload } from '../schemas/auth.schema';
